// script.js for Say_and_do_1A
// Simple module — GIFs are always playing (animated), no audio interaction needed.
// Page is purely display-based: header + scrollable GIF cards.

document.addEventListener('DOMContentLoaded', () => {
    // All GIFs are natively animated — no JS needed for playback.
    // This script is kept for future extensibility (e.g. adding audio interaction).
    console.log('Say and Do 1A module loaded.');
});
